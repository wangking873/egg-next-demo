import React, { Component } from 'react';
import { AppContainer } from '../components';

export default class extends Component {
  render() {
    return <AppContainer>
    <h1>Hello Blog!44444</h1>
  </AppContainer>;
  }
}